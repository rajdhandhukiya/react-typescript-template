import { primary } from 'constants/color.constant';

export const styles: any = {
  errorMain: {
    height: '100vh',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
  },
  errorLabel: {
    color: primary,
    fontSize: '18px',
    fontWeight: 'lighter',
    letterSpacing: '0.5px'
  }
};
